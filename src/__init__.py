"""Pacote raiz do agente GenAI (Prefeitura do Rio).

Expõe os namespaces principais:
- src.agent  → grafo e nós do agente (LangGraph)
- src.utils  → utilitários (BigQuery, logger, schema, LLM)
"""

__all__ = ["agent", "utils"]
